from src.controllers.bot.routers.user.test.handlers import (  # noqa — registers handlers
    open_selector, configure_selector, start_test, answer, explanation, navigate, next_part,
)
from src.controllers.bot.routers.user.test.router import register_filters as router